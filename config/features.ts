// IWESR remains in the project while its visuals are still under development.
// Change this single flag to true when the module is ready for frontend review.
export const ENABLE_IWESR = false;

// Keep the alert UI and data intact while hiding the floating entry point.
export const ENABLE_ALERT_ENTRY = false;
